PORT=3001
COPILOT_DEFAULT_MODEL=auto

# Optional: point SDK to an external headless Copilot CLI (host:port)
# COPILOT_CLI_URL=localhost:4321

# Optional: service-token auth per docs
# COPILOT_GITHUB_TOKEN=gho_xxx
